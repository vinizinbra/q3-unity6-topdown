// Tag component granted/revoked by OverdriveInstantReloadSkillAction (Begin/End paired) - presence
// means the owner has this upgrade equipped for the current Berserk activation. No fields:
// WeaponSystem also checks RageOverdrive.Overdriven before actually zeroing the reload time, so
// the benefit only kicks in once max Rage is actually reached, not for the whole Berserk window -
// this component only needs to say "is the upgrade equipped at all".
component InstantReloadOverdrive
{
}
